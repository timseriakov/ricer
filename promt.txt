Я переименовываю проект. Прошу тебя пройтись по всей кодовой базе и сделать следующие замены:

1. В файлах go.mod, import-путях и других упоминаниях:
   - Замени старый go module `zeshi09/gofi-launcher` на `timseriakov/ricer`

2. Во всех кодовых местах, комментариях, логах и документации:
   - Замени название проекта `gofi-launcher` на `ricer`
   - Обнови пути конфигурации, если где-то используется `~/.config/gofi-launcher/`, замени на `~/.config/ricer/`

3. Обнови:
   - `go.mod`: `module timseriakov/ricer`
   - Все import-пути `zeshi09/gofi-launcher/...` → `timseriakov/ricer/...`
   - README.md и flake.nix — обнови имя проекта, описание и команды

4. Не меняй внутреннюю логику приложения, только названия, пути и импорты. Убедись, что проект после замены компилируется.

После этого я хочу использовать `ricer` как основное имя CLI, папки, конфига и import-модуля.
